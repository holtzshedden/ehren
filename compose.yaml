services:
  ehren:
    image: node:24-alpine
    working_dir: /app
    command: sh -c "cd /app && npm install && npm run dev -- --hostname 0.0.0.0"
    ports:
      - "3001:3000"
    volumes:
      - /volume1/carni/Websites/ehren:/app